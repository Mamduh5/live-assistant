# TikFinity fixtures

`synthetic-events.json` contains synthetic, minimal envelopes based on the documented TikFinity field semantics used by this adapter. These are not production captures.

The fixtures contain no real user identifiers, avatars, credentials, cookies, tokens, or authentication material. Real observed payloads may supplement them later after sanitization and explicit review.

